import uid from "./uid";
import {
  getColorFromPercent,
  getPixelsFromPercent,
  getColorFromTag,
} from "./convert";

import FlashMap from "./flashmap";

export {
  uid,
  FlashMap,
  getColorFromPercent,
  getPixelsFromPercent,
  getColorFromTag,
};
